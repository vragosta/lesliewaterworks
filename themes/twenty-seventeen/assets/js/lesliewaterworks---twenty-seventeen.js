( function() {
	jQuery( document ).ready( function( $ ) {
		var args = {
			autoplay: true,
			autoplaySpeed: 2500,
			dots: true,
			arrows: false
		}

		$( '#slick-slider' ).slick( args );

	} );
} )( this );
